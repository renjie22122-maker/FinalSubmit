# ChordAtlas upstream and project overlay

- Repository: <https://github.com/twak/chordatlas>
- Branch at capture: `stable`
- Baseline commit: `1e6f5cf174e30f7bd2e5a374ad70ced75fe75fd9`
- Licence: Apache License 2.0 (`LICENSE`)

This directory begins with an archive of the baseline commit. It is overlaid with the local Java modifications/additions used by the dissertation and with the `bridge/` integration package. The local `myProject` source directory did not contain Git history, so this baseline identifier and the contribution statement provide the explicit upstream/project boundary.

Generated JARs, local Eclipse state, project workspaces, imagery, datasets, logs, caches, Gurobi binaries, and model weights are excluded.
